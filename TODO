visual mode:
	exec-region = :%!$repl
	r[char], ~, and : over a region

operators:
	R - multi-char replacement
	paste
	undo
	y
	g~, gu, gU, gq
	=
	!

insert:
	^U, ^W
	^V

motions:
	%
	n, N
	[[, [{
	i/a: iw, i{, ip
	operator-pending:
		v, V, c-v


cmds:
	:! / cmd parsing
	save-check  on :q/e
	exist-check on :w
	:e - limit cursor to (x, max_lines) when loading a new file
	zt, zb, zz
