FIXES:
	indent - when not in visual line mode, the last line isn't indented
	       - see M_EXCLUSIVE code, around_motion(), after r.type set

visual mode:
	exec-region = :%!$repl
	n number of cursor points - multi/mark insert?
	r[char], ~, and : over a region

keys:
	check:
		dd, yy, cc
	s
	R - multi-char replacement
	k_format, k_paste, k_undo, k_yank

motions:
	%
	n, N
	[[
	i:
		iw
		i{

cmds:
	:! / cmd parsing
	save-check  on :q/e
	exist-check on :w
	:e - limit cursor to (x, max_lines) when loading a new file
